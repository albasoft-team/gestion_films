{% extends 'base.html.twig' %}

{% block body %}
    <a class="btn btn-default" href="{{ path('film_index') }}">Back to the list</a>
    <h1>Modification d'un film</h1>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form) }}
        <input class="btn btn-success" style="margin-top: 10px" type="submit" value="Modifier" />
    {{ form_end(edit_form) }}

    {#<ul>#}
        {#<li>#}
            {#<a href="{{ path('film_index') }}">Back to the list</a>#}
        {#</li>#}
        {#<li>#}
            {#{{ form_start(delete_form) }}#}
                {#<input type="submit" value="Delete">#}
            {#{{ form_end(delete_form) }}#}
        {#</li>#}
    {#</ul>#}
{% endblock %}
